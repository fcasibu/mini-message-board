extends layout

block content
  h1= title
  form(action='/new', method='POST')
    input(type='text', name='name', placeholder='Name', required)
    textarea(name='messageText', placeholder='Message', required)
    button(type='submit') submit